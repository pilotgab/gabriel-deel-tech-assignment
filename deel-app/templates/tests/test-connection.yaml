apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deel-app.fullname" . }}-test-connection"
  labels:
    {{- include "deel-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deel-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
